package com.estudio.oss_dns_resolver_v1.no_di.network;

import static android.content.Context.MODE_PRIVATE;
import static org.junit.Assert.assertEquals;

import android.content.Context;
import android.content.SharedPreferences;

import androidx.test.platform.app.InstrumentationRegistry;

import com.estudio.oss_dns_resolver_v1.utils.Constants;

import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;

public class CustomInterceptorTest {


    @Test
    public void intercept() {


//        HttpServiceBuilder builder = new HttpServiceBuilder.Builder()
//                .setURL("https://google.com:8303/api/v1/login")
//                .build();
//
//        String newURL = CustomInterceptor.intercept(builder).getURL();
//
//        assertEquals("https://facebook.com:8303/api/v1/login", newURL);
    }
}